# Styled GitHub Pages Version

This version keeps the same root-folder structure for your images and markdown files, but adds:

- `_config.yml`
- `_layouts/default.html`
- `assets/css/style.css`
- front matter on each page
- a cleaner homepage and navigation

Upload the whole folder to GitHub, then enable GitHub Pages from the `main` branch and `/(root)`.
